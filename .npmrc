# Obsidian requires the release tag to match manifest.json's version exactly,
# with no leading "v". npm's default prefix is "v", so clear it.
tag-version-prefix=""

# npm version's default commit subject is a bare "1.0.3"; match the repo's
# conventional-commit history instead. Also used as the annotated tag message.
message="chore(release): version %s"
